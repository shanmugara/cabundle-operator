apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cabundle-sync.fullname" . }}-test-connection"
  labels:
    {{- include "cabundle-sync.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cabundle-sync.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
