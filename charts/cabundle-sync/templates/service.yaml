apiVersion: v1
kind: Service
metadata:
  name: {{ include "cabundle-sync.fullname" . }}
  labels:
    {{- include "cabundle-sync.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cabundle-sync.selectorLabels" . | nindent 4 }}
